# Fase 6 — Operadores Aritméticos

## Objetivo
Resolver a fase usando 2 ou mais operadores, variáveis encadeadas e saltos.

## Comandos
- `move_right(var)`
- `move_left(var)`
- `jump()`

## Dica
Use `+`, `-` e `*` para calcular as distâncias antes de mover.
