# Fase 5 — Cálculo de Rota

## Objetivo
Usar variáveis com `+` e `-` para montar uma rota completa de coleta.

## Comandos
- `move_right(var)`
- `move_left(var)`

## Dica
Quebre a rota em partes: ida, ajuste e retorno.
