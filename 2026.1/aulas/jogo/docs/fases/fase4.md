# Fase 4 — Variáveis e Tipos

## Objetivo
Declarar variáveis e usar uma variável numérica para mover o personagem.

## Comandos
- `move_right(var)`
- `move_left(var)`

## Dica
Comece com variáveis simples, por exemplo: `passos = 3`.
